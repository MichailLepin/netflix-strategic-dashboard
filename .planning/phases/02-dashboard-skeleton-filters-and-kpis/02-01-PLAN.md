---
phase: 02-dashboard-skeleton-filters-and-kpis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .streamlit/config.toml
  - assets/netflix_logo.png
  - app.py
autonomous: true
requirements: [DASH-01, DASH-02]

must_haves:
  truths:
    - "Netflix logo and dashboard title are visible at the top of the app"
    - "App uses wide layout with no horizontal scrolling"
    - "Dark theme (dark background, light text) applies on first load without flash"
    - "Placeholder areas exist for KPI row and chart grid"
  artifacts:
    - path: ".streamlit/config.toml"
      provides: "Netflix dark theme declaration"
      contains: "base = \"dark\""
    - path: "app.py"
      provides: "Streamlit entry point with layout skeleton"
      min_lines: 30
    - path: "assets/netflix_logo.png"
      provides: "Netflix logo image or text fallback"
  key_links:
    - from: "app.py"
      to: "src/data_loader.py"
      via: "from src.data_loader import load_data"
      pattern: "from src\\.data_loader import load_data"
    - from: "app.py"
      to: ".streamlit/config.toml"
      via: "Streamlit auto-reads config.toml on startup"
      pattern: "config\\.toml"
---

<objective>
Create the Streamlit app shell with Netflix dark branding, wide layout, header row (logo + title), and placeholder areas for KPI tiles and charts.

Purpose: Establish the visual frame that Phase 2 Plan 02 will wire filters and KPIs into, and Phase 3 will fill with charts.
Output: A runnable `streamlit run app.py` that shows the Netflix-branded skeleton with placeholder content.
</objective>

<execution_context>
@C:/Users/lepin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lepin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-skeleton-filters-and-kpis/02-CONTEXT.md
@.planning/phases/02-dashboard-skeleton-filters-and-kpis/02-RESEARCH.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From src/data_loader.py — the only import needed -->
From src/data_loader.py:
```python
@st.cache_data
def load_data() -> dict[str, pd.DataFrame]:
    """Returns dict with keys: users, movies, watch, recs, searches, reviews"""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.toml dark theme and assets directory</name>
  <files>.streamlit/config.toml, assets/netflix_logo.png</files>
  <action>
1. Create `.streamlit/config.toml` with Netflix dark theme:
   ```toml
   [theme]
   base = "dark"
   primaryColor = "#E50914"
   backgroundColor = "#141414"
   secondaryBackgroundColor = "#1F1F1F"
   textColor = "#FFFFFF"
   font = "sans serif"
   ```

2. Create `assets/` directory. For the Netflix logo, use a text-based fallback approach: create a small helper that renders `st.markdown('<h1 style="color:#E50914;font-weight:900;margin:0;">NETFLIX</h1>', unsafe_allow_html=True)` in app.py. Do NOT download a real Netflix logo (license issues for academic work). If desired later, a PNG can be dropped into assets/ and the code switched to `st.image()`.

   Create a minimal placeholder file `assets/.gitkeep` so the directory is tracked.
  </action>
  <verify>
    <automated>test -f .streamlit/config.toml && grep -q 'base = "dark"' .streamlit/config.toml && grep -q "E50914" .streamlit/config.toml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>config.toml exists with Netflix dark theme colors. assets/ directory exists.</done>
</task>

<task type="auto">
  <name>Task 2: Create app.py with layout skeleton</name>
  <files>app.py</files>
  <action>
Create `app.py` as the Streamlit entry point. This is the FIRST file users run (`streamlit run app.py`).

**CRITICAL: `st.set_page_config()` must be the very first `st.` call in the file.** Import streamlit, call set_page_config immediately, then import everything else.

Structure:
```python
import streamlit as st

st.set_page_config(
    page_title="Netflix Strategic Dashboard",
    page_icon=":clapper:",   # emoji fallback, no PNG dependency
    layout="wide",
    initial_sidebar_state="expanded",
)

from src.data_loader import load_data

# Load data (cached)
data = load_data()

# --- Header Row ---
# Use st.columns([1, 6]) for logo + title
# Left column: text-based NETFLIX logo in red (#E50914) via st.markdown with unsafe_allow_html=True
# Right column: st.markdown("## Netflix Strategic Dashboard") + st.caption("Content Strategy & User Engagement Analytics")

# --- Custom CSS for metric tiles ---
# Inject CSS using st.markdown with unsafe_allow_html=True:
# - [data-testid="stMetric"] gets dark background (#1F1F1F), border-radius 8px, padding 16px, left border 4px solid #E50914
# - [data-testid="stMetricValue"] gets font-size 2rem, font-weight 700

# --- Sidebar placeholder ---
# with st.sidebar:
#     st.header("Filters")
#     st.info("Filters will be added in the next step")

# --- KPI Row placeholder ---
# st.markdown("---")
# kpi_cols = st.columns(4)
# for i, col in enumerate(kpi_cols):
#     with col:
#         st.metric(f"KPI {i+1}", "—")

# --- Chart Grid placeholder ---
# st.markdown("---")
# st.subheader("Content & Engagement Analytics")
# chart_col1, chart_col2 = st.columns(2)
# with chart_col1:
#     st.info("Chart: Engagement Trends Over Time (Phase 3)")
# with chart_col2:
#     st.info("Chart: Top Genres by Watch Time (Phase 3)")
# chart_col3, chart_col4 = st.columns(2)
# with chart_col3:
#     st.info("Chart: Device Type Distribution (Phase 3)")
# with chart_col4:
#     st.info("Chart: Watch Time vs Content Rating (Phase 3)")
```

IMPORTANT: The comments above show the structure — implement them as REAL CODE, not comments. The sidebar and KPI placeholders use real st.metric/st.info calls so the layout is visible when running the app.

Per user decision: dark theme, Netflix red #E50914, title "Netflix Strategic Dashboard", subtitle "Content Strategy & User Engagement Analytics".
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('app.py').read()); print('SYNTAX OK')" && grep -q "layout.*wide" app.py && grep -q "set_page_config" app.py && grep -q "load_data" app.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>app.py is syntactically valid, uses layout="wide", calls set_page_config first, imports load_data, renders header with Netflix branding, shows placeholder KPI row and chart grid.</done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('app.py').read())"` — syntax valid
2. `grep -q 'layout.*wide' app.py` — wide layout configured
3. `grep -q 'E50914' app.py` — Netflix red color present
4. `grep -q 'set_page_config' app.py` — page config called
5. `.streamlit/config.toml` exists with dark theme
</verification>

<success_criteria>
- `streamlit run app.py` launches without errors and shows Netflix-branded dark skeleton
- Header shows NETFLIX text logo in red + "Netflix Strategic Dashboard" title
- 4 placeholder KPI tiles visible in a row
- 4 placeholder chart areas visible in a 2x2 grid
- No vertical scrolling needed at 1080p
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-skeleton-filters-and-kpis/02-01-SUMMARY.md`
</output>
