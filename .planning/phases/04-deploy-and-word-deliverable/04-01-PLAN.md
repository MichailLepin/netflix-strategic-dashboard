---
phase: 04-deploy-and-word-deliverable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - runtime.txt
  - app.py
  - generate_report.py
  - Lepin_Partner.docx
autonomous: true
requirements:
  - DASH-03
  - DLVR-01
  - DLVR-02

must_haves:
  truths:
    - "requirements.txt lists all runtime dependencies (streamlit, plotly, pandas) without test-only deps"
    - "runtime.txt pins Python version for Streamlit Cloud"
    - "Alarm threshold descriptions are visible in the dashboard sidebar"
    - "Word file exists with all required sections (business requirements, data prep, alarm descriptions, placeholder for screenshot and link)"
  artifacts:
    - path: "requirements.txt"
      provides: "Deployment dependencies"
      contains: "streamlit"
    - path: "runtime.txt"
      provides: "Python version pin"
      contains: "python-3.12"
    - path: "generate_report.py"
      provides: "Script to generate Word deliverable"
      min_lines: 80
    - path: "Lepin_Partner.docx"
      provides: "Generated Word deliverable"
    - path: "app.py"
      provides: "Dashboard with alarm descriptions in sidebar"
      contains: "Alarm Thresholds"
  key_links:
    - from: "generate_report.py"
      to: "Lepin_Partner.docx"
      via: "python-docx document generation"
      pattern: "document\\.save"
    - from: "app.py"
      to: "src/kpis.py"
      via: "alarm threshold display references same thresholds as get_alarm_level()"
      pattern: "Alarm"
---

<objective>
Prepare all deployment artifacts and generate the Word deliverable for the Netflix Strategic Dashboard assignment.

Purpose: Make the project deployment-ready and produce the required academic deliverable document.
Output: Updated requirements.txt, runtime.txt, alarm descriptions in dashboard sidebar, generate_report.py script, and Lepin_Partner.docx Word file.
</objective>

<execution_context>
@C:/Users/lepin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lepin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deploy-and-word-deliverable/04-CONTEXT.md

Source files:
@app.py
@src/kpis.py
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare deployment files and add alarm descriptions to dashboard</name>
  <files>requirements.txt, runtime.txt, app.py</files>
  <action>
1. Update `requirements.txt` — remove `pytest>=8.0` (test-only, not needed on Streamlit Cloud). Keep: streamlit==1.55.0, plotly==6.6.0, pandas==2.3.3. Add: python-docx (needed for report generation but can be removed after generation — keep it for reproducibility).

2. Create `runtime.txt` with content: `python-3.12` (single line, no trailing whitespace). This pins the Python version for Streamlit Cloud deployment.

3. Add alarm threshold descriptions to the dashboard sidebar in `app.py`. After the existing filter widgets in the `with st.sidebar:` block, add a new section:

```python
    st.markdown("---")
    st.subheader("Alarm Thresholds")
    st.markdown("""
    **Avg Watch Time**
    - :green[Green] > 2 hrs | :orange[Yellow] 1-2 hrs | :red[Red] < 1 hr
    - *Action: Evaluate content engagement strategies*

    **Completion Rate**
    - :green[Green] > 60% | :orange[Yellow] 40-60% | :red[Red] < 40%
    - *Action: Review content quality and pacing*

    **Rec Click-Through**
    - :green[Green] > 20% | :orange[Yellow] 10-20% | :red[Red] < 10%
    - *Action: Tune recommendation algorithm*

    **Churn Rate**
    - :green[Green] < 15% | :orange[Yellow] 15-25% | :red[Red] > 25%
    - *Action: Investigate retention strategies*
    """)
```

These thresholds MUST match exactly what is in `src/kpis.py` `get_alarm_level()` function. Cross-reference before writing.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('app.py').read()); print('app.py syntax OK')" && cat runtime.txt && python -c "lines=open('requirements.txt').readlines(); assert any('streamlit' in l for l in lines); assert not any('pytest' in l for l in lines); print('requirements.txt OK')"</automated>
  </verify>
  <done>requirements.txt has only runtime deps, runtime.txt exists with python-3.12, app.py sidebar shows alarm threshold descriptions matching src/kpis.py thresholds</done>
</task>

<task type="auto">
  <name>Task 2: Create Word deliverable generator script and generate document</name>
  <files>generate_report.py, Lepin_Partner.docx</files>
  <action>
1. First ensure python-docx is installed: `pip install python-docx`

2. Create `generate_report.py` — a standalone script that generates `Lepin_Partner.docx` using python-docx. The document must contain these sections:

**Section 1: Business Requirements**
- Heading 1: "Business Requirements"
- Business problem: Netflix needs to monitor user engagement, content performance, and subscription health to make data-driven decisions about content strategy and user retention.
- Goals: Track 4 KPIs (Avg Watch Time, Completion Rate, Recommendation CTR, Churn Rate), identify trends in viewing behavior, segment analysis by genre/subscription/device/time period.
- Key questions: Which content genres drive the most engagement? Are recommendations effective at driving clicks? What is the churn risk level? How do engagement patterns vary across subscription tiers and devices?
- Target audience: VP of Product / Head of Content Strategy at Netflix.

**Section 2: Data Preparation Notes**
- Heading 1: "Data Preparation Notes"
- Dataset: Netflix 2025 User Behavior Dataset from Kaggle (210,000+ records across 6 tables: users, movies, watch_history, ratings, recommendation_logs, customer_support)
- Cleaning: Categorical dtype casting for memory optimization, datetime parsing for watch_date, boolean handling for is_active and was_clicked columns
- Variable summary table (use python-docx Table): list key columns used (user_id, watch_duration_minutes, progress_percentage, genre_primary, subscription_plan, device_type, watch_date, is_active, was_clicked, user_rating) with their types and descriptions
- Join strategy: All tables joined via user_id (100% match rate on all join keys)

**Section 3: Dashboard Screenshot**
- Heading 1: "Dashboard Screenshot"
- Placeholder paragraph in italic: "[INSERT SCREENSHOT: Take a screenshot of the dashboard from the deployed Streamlit Cloud URL (not localhost) and paste it here]"

**Section 4: Public Link**
- Heading 1: "Public Link"
- Placeholder paragraph: "[INSERT LINK: Paste your Streamlit Cloud URL here, e.g., https://your-app.streamlit.app]"

**Section 5: Dashboard and Alarm Description**
- Heading 1: "Dashboard and Alarm Description"
- Sub-section describing dashboard elements: sidebar filters (genre, subscription type, device type, date range), KPI scorecard tiles with color-coded alarm borders, 4 charts (engagement line chart, genre bar chart, device donut chart, rating scatter plot)
- Sub-section "Alarm Thresholds and Recommended Actions" with a table:
  | KPI | Green | Yellow | Red | Recommended Action |
  | Avg Watch Time | > 2 hrs | 1-2 hrs | < 1 hr | Evaluate content engagement strategies |
  | Completion Rate | > 60% | 40-60% | < 40% | Review content quality and pacing |
  | Rec Click-Through | > 20% | 10-20% | < 10% | Tune recommendation algorithm |
  | Churn Rate | < 15% | 15-25% | > 25% | Investigate retention strategies |
- Explain that alarm colors appear as left-border colors on KPI tiles: green = healthy, yellow = warning, red = critical.

**Section 6: Peer Review**
- Heading 1: "Peer Review"
- Placeholder paragraph: "[This section is for reviewing another team's dashboard — to be completed separately]"

**Formatting:**
- Use Heading 1 for main sections, Heading 2 for sub-sections
- Use python-docx Table objects for tabular data (variable summary, alarm thresholds)
- Normal paragraphs for body text
- Bold for emphasis on key terms
- Document title at top: "Netflix Strategic Dashboard — Project Report"
- Authors line: "Mikhail Lepin & [Partner Name]"

3. Run the script: `python generate_report.py` to produce `Lepin_Partner.docx`

4. Verify the file was created and is not empty.
  </action>
  <verify>
    <automated>python generate_report.py && python -c "from docx import Document; d=Document('Lepin_Partner.docx'); headings=[p.text for p in d.paragraphs if p.style.name.startswith('Heading')]; print('Headings:', headings); assert len(headings) >= 6, f'Expected 6+ headings, got {len(headings)}'; assert any('Alarm' in h for h in headings); print('PASS')"</automated>
  </verify>
  <done>Lepin_Partner.docx exists with all 6 sections (Business Requirements, Data Preparation, Screenshot placeholder, Public Link placeholder, Dashboard and Alarm Description with threshold table, Peer Review placeholder). File opens correctly in Word.</done>
</task>

</tasks>

<verification>
1. `streamlit run app.py` launches without import errors (sanity check)
2. Sidebar shows "Alarm Thresholds" section with correct threshold values
3. `Lepin_Partner.docx` opens and contains all required sections
4. `requirements.txt` has no test-only dependencies
5. `runtime.txt` contains `python-3.12`
</verification>

<success_criteria>
- All deployment files ready (requirements.txt clean, runtime.txt present)
- Alarm descriptions visible in dashboard sidebar matching src/kpis.py thresholds
- Word deliverable generated with 6 sections including alarm threshold table
- Project is ready for GitHub push and Streamlit Cloud deployment (Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/04-deploy-and-word-deliverable/04-01-SUMMARY.md`
</output>
